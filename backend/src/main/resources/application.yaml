spring:
  application:
    name: backend
  config:
    import: classpath:/config/db.yml,classpath:/config/redis.yml
  profiles:
    active: dev


mybatis:
  mapper-locations: classpath:mappers/**/*.xml